<div class="min-h-screen bg-stevensBg font-body text-stevensGray">
  <div class="max-w-5xl mx-auto px-6 py-10">
    <h1 class="text-3xl font-heading text-stevensMaroon mb-8">Report Dashboard</h1>

    {{!-- FORUMS --}}
    <section class="mb-12">
      <h2 class="text-2xl font-heading text-stevensMaroon mb-2">Forums</h2>
      <hr class="mb-4"/>
      {{#if forumReports.length}}
        {{#each forumReports as |report|}}
          <a href="/forums/{{report.forumId}}" class="block group">
            <div class="bg-stevensWhite rounded-2xl shadow p-6 mb-6 hover:shadow-lg transition cursor-pointer">

              <!-- Reason -->
              <p class="text-sm font-medium text-stevensGray">Reason</p>
              <p class="text-lg font-medium mb-4">{{report.reason}}</p>

              <div class="grid grid-cols-2 gap-4 text-sm text-stevensGray">
                <!-- Status -->
                <div>
                  <p class="font-medium">Status</p>
                  <span class="inline-flex items-center px-3 py-1 rounded-full
                    {{#if (eq report.status 'resolved')}}bg-green-100 text-green-800{{/if}}
                    {{#if (eq report.status 'under review')}}bg-yellow-100 text-yellow-800{{/if}}
                    {{#if (eq report.status 'rejected')}}bg-red-100 text-red-800{{/if}}">
                    {{report.status}}
                  </span>
                </div>
                <!-- Date -->
                <div>
                  <p class="font-medium">Date</p>
                  <p>{{formatDate report.createdAt}}</p>
                </div>
              </div>

            </div>
          </a>
        {{/each}}
      {{else}}
        <p class="text-stevensGray">No forum reports.</p>
      {{/if}}
    </section>

    {{!-- POLLS --}}
    <section class="mb-12">
      <h2 class="text-2xl font-heading text-stevensMaroon mb-2">Polls</h2>
      <hr class="mb-4"/>
      {{#if pollReports.length}}
        {{#each pollReports as |report|}}
          <a href="/polls/{{report.pollId}}" class="block group">
            <div class="bg-stevensWhite rounded-2xl shadow p-6 mb-6 hover:shadow-lg transition cursor-pointer">

              <!-- Reason -->
              <p class="text-sm font-medium text-stevensGray">Reason</p>
              <p class="text-lg font-medium mb-4">{{report.reason}}</p>

              <div class="grid grid-cols-2 gap-4 text-sm text-stevensGray">
                <!-- Status -->
                <div>
                  <p class="font-medium">Status</p>
                  <span class="inline-flex items-center px-3 py-1 rounded-full
                    {{#if (eq report.status 'resolved')}}bg-green-100 text-green-800{{/if}}
                    {{#if (eq report.status 'under review')}}bg-yellow-100 text-yellow-800{{/if}}
                    {{#if (eq report.status 'rejected')}}bg-red-100 text-red-800{{/if}}">
                    {{report.status}}
                  </span>
                </div>
                <!-- Date -->
                <div>
                  <p class="font-medium">Date</p>
                  <p>{{formatDate report.createdAt}}</p>
                </div>
              </div>

            </div>
          </a>
        {{/each}}
      {{else}}
        <p class="text-stevensGray">No poll reports.</p>
      {{/if}}
    </section>

    {{!-- REVIEWS --}}
    <section class="mb-12">
      <h2 class="text-2xl font-heading text-stevensMaroon mb-2">Reviews</h2>
      <hr class="mb-4"/>
      {{#if reviewReports.length}}
        {{#each reviewReports as |report|}}
          <a href="/userSideCourses/reviews/{{report.reviewId}}" class="block group">
            <div class="bg-stevensWhite rounded-2xl shadow p-6 mb-6 hover:shadow-lg transition cursor-pointer">

              <!-- Reason -->
              <p class="text-sm font-medium text-stevensGray">Reason</p>
              <p class="text-lg font-medium mb-4">{{report.reason}}</p>

              <div class="grid grid-cols-2 gap-4 text-sm text-stevensGray">
                <!-- Status -->
                <div>
                  <p class="font-medium">Status</p>
                  <span class="inline-flex items-center px-3 py-1 rounded-full
                    {{#if (eq report.status 'resolved')}}bg-green-100 text-green-800{{/if}}
                    {{#if (eq report.status 'under review')}}bg-yellow-100 text-yellow-800{{/if}}
                    {{#if (eq report.status 'rejected')}}bg-red-100 text-red-800{{/if}}">
                    {{report.status}}
                  </span>
                </div>
                <!-- Date -->
                <div>
                  <p class="font-medium">Date</p>
                  <p>{{formatDate report.createdAt}}</p>
                </div>
              </div>

            </div>
          </a>
        {{/each}}
      {{else}}
        <p class="text-stevensGray">No review reports.</p>
      {{/if}}
    </section>

    {{!-- RESOURCES --}}
    <section>
      <h2 class="text-2xl font-heading text-stevensMaroon mb-2">Resources</h2>
      <hr class="mb-4"/>
      {{#if academicResourceReports.length}}
        {{#each academicResourceReports as |report|}}
          <a href="/academicResources/{{report.academicResourceId}}" class="block group">
            <div class="bg-stevensWhite rounded-2xl shadow p-6 mb-6 hover:shadow-lg transition cursor-pointer">

              <!-- Reason -->
              <p class="text-sm font-medium text-stevensGray">Reason</p>
              <p class="text-lg font-medium mb-4">{{report.reason}}</p>

              <div class="grid grid-cols-2 gap-4 text-sm text-stevensGray">
                <!-- Status -->
                <div>
                  <p class="font-medium">Status</p>
                  <span class="inline-flex items-center px-3 py-1 rounded-full
                    {{#if (eq report.status 'resolved')}}bg-green-100 text-green-800{{/if}}
                    {{#if (eq report.status 'under review')}}bg-yellow-100 text-yellow-800{{/if}}
                    {{#if (eq report.status 'rejected')}}bg-red-100 text-red-800{{/if}}">
                    {{report.status}}
                  </span>
                </div>
                <!-- Date -->
                <div>
                  <p class="font-medium">Date</p>
                  <p>{{formatDate report.createdAt}}</p>
                </div>
              </div>

            </div>
          </a>
        {{/each}}
      {{else}}
        <p class="text-stevensGray">No resource reports.</p>
      {{/if}}
    </section>

  </div>
</div>
